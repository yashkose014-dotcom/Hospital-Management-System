import { HeartPulse, LockKeyhole } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { useState } from 'react'
const roles = ['ADMIN','DOCTOR','RECEPTIONIST','PATIENT']
export default function LoginPage(){const [role,setRole]=useState('PATIENT');const navigate=useNavigate();return <div className="min-h-screen bg-brand-50 px-4 py-10"><div className="mx-auto max-w-md"><div className="mb-8 flex items-center justify-center gap-2 text-2xl font-bold text-brand-900"><HeartPulse/> MediCare</div><form onSubmit={e=>{e.preventDefault();navigate(`/dashboard/${role.toLowerCase()}`)}} className="card"><h1 className="text-2xl font-bold">Welcome back</h1><p className="mt-2 text-sm text-slate-500">Sign in to access your care portal.</p><label className="mt-6 block text-sm font-semibold">Email<input type="email" className="mt-2 w-full rounded-lg border border-slate-200 px-3 py-3 font-normal" placeholder="name@example.com" required/></label><label className="mt-4 block text-sm font-semibold">Password<input type="password" className="mt-2 w-full rounded-lg border border-slate-200 px-3 py-3 font-normal" placeholder="••••••••" required/></label><label className="mt-4 block text-sm font-semibold">Preview role<select value={role} onChange={e=>setRole(e.target.value)} className="mt-2 w-full rounded-lg border border-slate-200 px-3 py-3 font-normal">{roles.map(r=><option key={r}>{r}</option>)}</select></label><button className="btn-primary mt-6 w-full"><LockKeyhole className="mr-2" size={18}/> Sign in</button><p className="mt-4 text-center text-xs text-slate-500">Demo navigation only. JWT authentication will connect here.</p><p className="mt-4 text-center text-sm text-slate-500">New patient? <Link className="font-semibold text-brand-600" to="/register">Create an account</Link></p></form></div></div>}
