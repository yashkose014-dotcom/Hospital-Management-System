import { CalendarCheck, Clock3, Users, WalletCards } from 'lucide-react'
import { useParams } from 'react-router-dom'
const content={admin:{title:'Hospital overview',copy:'A snapshot of MediCare today.',stats:[['1,248','Patients',Users],['128','Appointments',CalendarCheck],['56','Doctors',Users],['₹2.4L','Revenue',WalletCards]]},doctor:{title:'Good morning, Doctor',copy:'You have 8 appointments scheduled for today.',stats:[['8','Today’s visits',CalendarCheck],['3','Pending notes',Clock3],['24','My patients',Users],['2','Lab reports',WalletCards]]},receptionist:{title:'Reception desk',copy:'Keep the day moving smoothly.',stats:[['18','Check-ins',Users],['12','Upcoming visits',CalendarCheck],['4','Waiting now',Clock3],['3','Payments due',WalletCards]]},patient:{title:'Your health dashboard',copy:'Your care, organised in one place.',stats:[['1','Upcoming visit',CalendarCheck],['3','Medical records',Users],['2','Prescriptions',WalletCards],['0','Outstanding bills',Clock3]]}}
export function DashboardHome(){const {role='patient'}=useParams();const item=content[role]||content.patient;return <><p className="text-sm font-semibold text-brand-600">{role.toUpperCase()} PORTAL</p><h1 className="mt-1 text-3xl font-bold text-slate-900">{item.title}</h1><p className="mt-2 text-slate-500">{item.copy}</p><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{item.stats.map(([num,label,Icon])=><div className="card" key={label}><Icon className="text-brand-600" size={20}/><p className="mt-5 text-3xl font-bold">{num}</p><p className="mt-1 text-sm text-slate-500">{label}</p></div>)}</div><div className="card mt-7"><h2 className="font-bold">Today’s activity</h2><div className="mt-5 space-y-4">{['Appointment updates will appear here once the API is connected.','Notifications and clinical workflow actions are role-specific.','Use the sidebar to explore the shared dashboard structure.'].map((s,i)=><div className="flex gap-3 text-sm text-slate-600" key={s}><span className="mt-0.5 h-5 w-5 rounded-full bg-blue-100 text-center text-xs leading-5 text-brand-600">{i+1}</span>{s}</div>)}</div></div></>}
export function DashboardSection(){const {role='patient',section='appointments'}=useParams();return <><p className="text-sm font-semibold text-brand-600">{role.toUpperCase()} PORTAL</p><h1 className="mt-1 capitalize text-3xl font-bold">{section}</h1><div className="card mt-7"><p className="font-semibold">{section[0].toUpperCase()+section.slice(1)} workspace</p><p className="mt-2 text-sm text-slate-500">This role-aware area is ready to be connected to the corresponding REST endpoint.</p></div></>}

